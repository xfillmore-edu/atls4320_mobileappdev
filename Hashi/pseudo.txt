// Display correct node for node value

val img: Drawable
when (node_value) {
	1 -> img = drawable.resource(is1) // not the right syntax but same idea
	2 -> 
	3 -> 
	4 -> 
	5 -> 
	6 -> 
	7 -> 
	8 -> 
	else -> // handle error case
}
return img


// call that node image switcher in fn passed matrix position
// while iterating through matrix?
// would assume global/class-level matrix variable

private fun mapNodeVisual(row, col) {
	
}

// use grid matrix (start with 10r x 8c)
